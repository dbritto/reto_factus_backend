# Validamos el formato del nombre de la rama
# (Si quieres mantener la validación de la rama, asegúrate que el script exista)

# Ejecutamos los tests antes de cada commit
npm test

# Ejecutamos el linter para mantener el código limpio en Cali
npm run lint